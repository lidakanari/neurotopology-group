---
/**
 * Section
 *
 * Provides consistent vertical spacing between
 * homepage and content sections.
 */

interface Props {
  class?: string;
  id?: string;
}

const {
  class: className = "",
  id
} = Astro.props;
---

<section
  id={id}
  class={`section ${className}`}
>
  <slot />
</section>